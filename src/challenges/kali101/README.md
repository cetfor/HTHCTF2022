# HTH CTF 2022 Kali 101

To build this you must specify `--network=host` like this:

```
docker build . --tag kali101 --network=host
```

Run to test with:

```
docker run -it kali101
```
